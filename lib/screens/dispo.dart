import "package:flutter/material.dart";

class Dispo extends StatelessWidget {
  const Dispo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}
